import { MatchPopulationPipe } from './match-population.pipe';

describe('MatchPopulationPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchPopulationPipe();
    expect(pipe).toBeTruthy();
  });
});
